{
  "buildCommand": "turbo run build --filter=clubedge",
  "devCommand": "turbo run dev --filter=clubedge --parallel",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "apps/clubedge/.next",
  "env": {
    "SENTRY_AUTH_TOKEN": "@ignored"
  }
}
